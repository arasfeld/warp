# For Expo SDK 54 + pnpm with isolated dependencies issues
# Use hoisted installation strategy (recommended for SDK 53, works for SDK 54)
# This prevents duplicate React/React Native versions that cause runtime errors
node-linker=hoisted
